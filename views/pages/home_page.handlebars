<a href="/login">Login</a>
<a href="/signup"> Sign Up</a>

<p>Welcome to Find A Doctor! Here you can search for doctors aswell as look at their reviews.</p>

<h2>Top Rated Doctors From Each Category</h2>
{{#if docs.length}}
<ul>
    {{#each docs}}
    <li>
        <h3 class="speciality">{{this.speciality}}</h3>
        <dl>
            <dd class="name">{{this.doctor.name}}</dd>
            {{#if this.doctor.profilePicture}}
            <img alt="image" src={{this.doctor.profilePicture}} />
            {{else}}
            <img alt="image"
                src="https://upload.wikimedia.org/wikipedia/commons/thumb/a/ac/No_image_available.svg/450px-No_image_available.svg.png" />
            {{/if}}

            <dt>About</dt>
            <dd>{{this.doctor.about}}</dd>
            <dt>Languages</dt>
            {{#if this.doctor.languages.length}}
            <ul>
                {{#each this.doctor.languages}}
                <li class="language">{{this}}</li>

                {{/each}}
            </ul>
            {{else}}
            <dd>N/A</dd>
            {{/if}}
            <dt>Address</dt>
            <dd>{{this.doctor.address}}</dd>
            <dt>City, State, Zipcode</dt>
            <dd>{{this.doctor.city}}, {{this.doctor.state}}, {{this.doctor.zip}}</dd>
            <dt>Rating</dt>
            <dd>{{this.doctor.rating}} Stars</dd>
    </li>
    {{/each}}
</ul>
{{else}}
<p class="not-found">We're sorry, but there are no current Doctors.</p>
{{/if}}
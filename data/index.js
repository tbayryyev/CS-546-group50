const usersData = require('./users');
const reviewsData = require('./reviews');
const doctorsData = require('./doctors');

module.exports = {
    users: usersData,
    reviews: reviewsData,
    doctors: doctorsData
};
